# 管理者用発信ボード機能 移植用プロンプト

以下の要件を満たす機能を、現在のシステムの構成・フレームワーク・デザイン規約に合わせて実装してください。
このプロンプトを一度実行するだけで、調査、実装、テスト、修正まで完了させてください。

## 目的

管理者が登録した「住民向け指示」と「避難所状況」を発信ボードで管理し、一般利用者はログインせずに発信内容を確認できるようにする。

## 実装前の確認

1. 既存の管理者認証、指示登録、避難所登録、テンプレート継承、データ保存の仕組みを確認する。
2. 既存の設計や命名規則を優先し、別のフレームワークや不要なデータベースを追加しない。
3. 既存データを壊さず、後方互換性を保つ。既存レコードに新しい項目がない場合も画面が正常に表示されるようにする。
4. 関係のない機能やファイルは変更しない。

## 必須機能

### 1. 管理者用フォーム

管理者がログインしている場合だけ、発信ボード上に以下のフォームを表示する。

#### 指示登録

次の項目を登録できるようにする。

- 指示内容
- 重要度: `緊急`、`警告`、`情報`
- 発信時間
- 対象地区
- 避難先（任意）

避難先は自由入力ではなく、現在システムに登録されている避難所を選択肢として表示する。
避難先を選択しないことも可能にする。

#### 避難所状況登録

次の項目を登録または更新できるようにする。

- 避難所名
- 残りの収容可能人数
- 物資の状況
- 状況メモ

避難所名が既存の避難所と一致する場合は、その避難所の状況を更新する。
存在しない場合の扱いは、既存システムの仕様に合わせる。ただし、避難所状況として発信ボードに表示できるようにする。

### 2. 発信ボード表示

住民向けの指示と避難所状況を、同じ発信ボードの一覧に表示する。

#### 指示カード

- 指示内容
- 重要度
- 対象地区
- 発信時間
- 避難先（設定されている場合のみ）
- 避難先の地図リンク（避難先が設定されている場合のみ）

避難先が設定されていない場合は、`避難先未設定`、`未指定` などの補助表示を出さない。

#### 避難所状況カード

- 避難所名
- 残りの収容可能人数
- 物資の状況
- 状況メモ
- 更新時刻

避難所状況は、指示の `緊急`、`警告`、`情報` とは明確に異なる専用カラーで表示する。
例として緑系を使用してよいが、既存デザインに調和する色を選ぶ。

#### 重要度の色分け

- `緊急`: 赤系
- `警告`: オレンジ系
- `情報`: 青系
- `避難所状況`: 上記3色とは異なる専用色

色だけに依存せず、カード上に重要度または種別名も表示する。

### 3. 地区絞り込み

発信ボードに地区フィルターを追加する。

- 全地区
- 現在登録されている住民向け指示の地区

選択した地区に該当する指示だけを表示する。
地区情報を持たない避難所状況の扱いは、既存仕様に合わせて決める。ただし、全地区では表示できるようにする。

### 4. 管理者による編集・削除

管理者は、発信ボード上で作成済みの指示と避難所状況を編集・削除できるようにする。

#### 指示の編集

以下を現在値入りのフォームで編集できるようにする。

- 指示内容
- 重要度
- 発信時間
- 対象地区
- 避難先

編集後は更新日時を更新する。

#### 指示の削除

- 対象IDを指定して削除する。
- 削除前に確認ダイアログを表示する。
- 削除後は一覧から消える。

#### 避難所状況の編集

以下を現在値入りのフォームで編集できるようにする。

- 残りの収容可能人数
- 物資の状況
- 状況メモ

避難所名や避難所本体は維持し、状況更新日時を更新する。

#### 避難所状況の削除

避難所そのものは削除せず、状況項目だけを削除または未設定状態に戻す。
削除後は発信ボードの避難所状況カードから消える。

#### 権限

編集・削除のPOST処理は、画面でボタンを隠すだけでなくサーバー側でも管理者認証を必須にする。
未ログインの操作はログイン画面へリダイレクトするか、適切な403エラーを返す。

### 5. 共通テンプレートへの連携

発信ボードの内容を、共通レイアウトである `base.html` にも表示する。

一般利用者がログインせずにホームやその他のページを開いた場合でも、住民向け指示と避難所状況を確認できるようにする。

`base.html` 側の発信表示は、発信ボードと同じ見た目・情報構造にする。

- 指示と避難所状況をカード形式で表示
- 緊急、警告、情報の優先度カラー
- 避難所状況の専用カラー
- 地区絞り込みフィルター
- 発信時間または更新時間
- 避難先（設定時のみ）

管理者用の編集・削除フォームは、一般利用者向けの共通表示には表示しない。
発信ボード本体では、管理者ログイン時のみ編集・削除操作を表示する。

## 確認済みチェック

`base.html` に表示する各発信カードの右側に、一般利用者が確認したかどうかを記録できるチェックボックスを配置する。

- ラベルは `確認済み` とする。
- チェックすると、そのカードを一覧の一番下へ移動する。
- 未確認カードは上部に残す。
- ページを再読み込みしても同じブラウザでは確認状態を維持する。
- サーバー側の発信データ自体は変更しない。
- 指示IDまたは避難所IDなど、安定した識別子を使って状態を管理する。
- 地区フィルターと確認済み表示が同時に正しく動作する。

利用者ごとのログイン情報を持たないシステムでは、ブラウザの `localStorage` など既存環境に適したクライアント側保存を使用する。

## データ設計

既存のデータ形式を尊重しつつ、必要に応じて次の情報を追加する。

- 指示: 一意なID、種別、重要度、地区、避難先、発信時間、作成日時、更新日時
- 避難所状況: 避難所ID、収容可能人数、物資状況、状況メモ、更新日時

指示と避難所状況を画面へ渡す際は、テンプレートで判別できる共通の表示用形式へ変換する。

既存の避難所登録データには状況項目がない場合があるため、未設定の避難所状況カードは表示しない。

## セキュリティと入力処理

- 編集・削除は管理者認証をサーバー側で検証する。
- IDはリクエスト値をそのまま信用せず、対象データの存在を確認する。
- 数値項目は不正な値で例外を起こさないように検証する。
- 入力値をHTMLへ表示する際は、使用フレームワークの標準エスケープを利用する。
- 削除処理では対象種別を明確にし、指示と避難所を誤って削除しない。

## 実装後の検証

次の項目を実際に確認し、失敗した場合は修正してから完了する。

1. アプリの構文チェックまたは型チェックが通る。
2. 一般利用者がログインせずにホームを開ける。
3. 一般利用者がログインせずに発信内容を確認できる。
4. 管理者だけに登録、編集、削除フォームが表示される。
5. 登録済み避難所が指示登録フォームの選択肢に表示される。
6. 指示を避難先付きで登録すると、避難先がカードに表示される。
7. 避難先なしの指示には、未設定を示す不要な表示が出ない。
8. 避難所状況を登録すると、発信ボードと `base.html` の共通表示に出る。
9. 緊急、警告、情報、避難所状況がそれぞれ意図した色で表示される。
10. 地区フィルターが指示と避難所状況に対して意図どおり動作する。
11. 指示の編集・削除が動作する。
12. 避難所状況の編集・削除が動作し、避難所本体は削除されない。
13. 未ログインで編集・削除POSTを送った場合に拒否される。
14. `base.html` の各カードの確認チェックで、確認済みカードが末尾へ移動する。
15. 再読み込み後も確認状態が維持される。
16. 差分に不要なファイル変更や空白エラーがない。

## 完了時の報告

実装完了後は、次の内容を簡潔に報告する。

- 変更したファイル
- 実装した機能
- 実行した検証コマンドと結果
- 未対応事項または既存環境に依存する事項
